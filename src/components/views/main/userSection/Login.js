import React from 'react';
import LoginForm from '../../../forms/LoginForm';

const Login = (props) => {
    return (
        <div className="login-form">
            <h4 className="mt-5 mb-5">Login</h4>
            <LoginForm></LoginForm>
        </div>
    )
}

export default Login;